import React, { FC } from "react";
import { HeaderStyles } from "./HeaderStyles";

const Navbar: FC = () => {
  return <div css={HeaderStyles.HeaderWrap}>Empty Header</div>;
};

export default Navbar;
